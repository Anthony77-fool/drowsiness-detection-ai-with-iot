# Force both languages to be detectable and treated as primary
*.cpp linguist-detectable=true
*.py linguist-detectable=true

# Optional: If you have large C++ libraries or assets (like a 'vendor' or 'lib' folder) 
# that are making the C++ percentage too high, hide them from the stats:
# path/to/library/* linguist-vendored
